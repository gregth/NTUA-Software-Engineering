const BaseController = require('./base')

module.exports = class PricesController extends BaseController {
    list(req, res) {
        res.sendStatus(204)
    }

    create(req, res) {
        res.sendStatus(204)
    }
}
